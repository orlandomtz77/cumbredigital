# Copia este archivo a .env y pon tu contraseña de la base de datos.
# Usa la cadena del "Session pooler" de Supabase (Dashboard → Connect → Session pooler, puerto 5432).
# En Render no se usa este archivo: agrega DATABASE_URL en Environment.
DATABASE_URL=postgresql://postgres.lsqycnimcwrmetyhykol:<CONTRASEÑA>@<HOST-DEL-POOLER>.pooler.supabase.com:5432/postgres?sslmode=require
